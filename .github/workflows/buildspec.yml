# name: API Deployment

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - main
#       - production
#       - staging
#       - development

# jobs:
#   runDeploycommand:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v3

#     - name: Configure AWS Credentials
#       uses: aws-actions/configure-aws-credentials@v1
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: ${{ secrets.AWS_REGION }}

#     - name: RunCommand
#       run: aws ssm send-command --document-name "AWS-RunShellScript" --parameters 'commands=["sudo -u ubuntu /bin/bash /home/ubuntu/deploy.sh dev velan-api"]' --targets "Key=instanceids,Values=${{ secrets.INSTANCE_ID }}" --comment "echo DeployScript"
